[project]
name = "rerun-simple-gs"
version = "0.1.0"
description = "Python helper code for the rerun-simple-gs Gaussian splat viewer example."
requires-python = ">=3.12"
dependencies = [
  "numpy",
  "plyfile>=1.1,<2",
  "rerun-sdk==0.30.0",
]

[tool.pyrefly]
project-includes = ["python"]
